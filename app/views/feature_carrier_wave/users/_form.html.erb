<%= form_with(model: feature_carrier_wave_user, local: true) do |form| %>
  <% if feature_carrier_wave_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(feature_carrier_wave_user.errors.count, "error") %> prohibited this feature_carrier_wave_user from being saved:</h2>

      <ul>
      <% feature_carrier_wave_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :avatar %>
    <%= form.file_field :avatar %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
